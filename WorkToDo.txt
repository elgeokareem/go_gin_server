Get a way to init the DB
  - We should have a set of tables (Done)
  - If we don't have the tables, we should create them (Done)

How the app should work?
  - We will have users (with email, password and binance token) (Done)
  - Each user will be a tenant (Not sure, will be working for me for now)

Tenants DBs
  - name convention for db will be db_<user_id>
  - We will have a table for each user's wallet

DB model
  binance_spot_wallet
    id, userId, asset, free, locked, freeze, created_at, updated_at

  binance_fund_wallet
    id, userId, asset, free, locked, created_at, updated_at

  users
    id, email, password, binance_token, created_at, updated_at

  # New
  This table will have the total current amount of money in USD. In the future we can have the user to change it.
  current_funds/close-over-shift-times
    id, userId, asset, amount, created_at, updated_at

It will be a table for all users.